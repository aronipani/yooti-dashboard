# DASH-009-T002 — Productivity tab and DORA tab

## Status: COMPLETE
## Layer: frontend
## Story: DASH-009 — Six-tab React dashboard
## Complexity: L (task 2 of 4)

## Depends on
- DASH-009-T001 (shared components must exist)

## AC covered
- AC-1 (Productivity and DORA tabs render with real data)
- AC-2 (insight strip on Productivity tab with correct severity)
- AC-3 (responsive at 320px)
- AC-5 (zero axe-core violations)

## Files in Scope

### CREATE
- `frontend/dashboard/src/pages/ProductivityTab.tsx`
- `frontend/dashboard/src/pages/DoraTab.tsx`
- `frontend/dashboard/src/components/DashboardLayout.tsx` — tab navigation, sprint selector
- `frontend/dashboard/tests/unit/ProductivityTab.test.tsx`
- `frontend/dashboard/tests/unit/DoraTab.test.tsx`
- `frontend/dashboard/tests/unit/DashboardLayout.test.tsx`

### MODIFY
None

### OUT OF SCOPE
- Quality and Agent efficiency tabs (T003)
- Value and Story drill-down tabs (T004)
- Shared components (T001)

## Implementation steps

1. Create `DashboardLayout.tsx`:
   - Tab navigation bar with 6 tabs
   - SprintSelector in header area
   - Active tab renders in content area
   - Responsive layout — stacked on mobile
2. Write failing tests for ProductivityTab
3. Create `ProductivityTab.tsx`:
   - KPI cards: cycle_time_avg, sprint_completion_pct, stories_shipped, stories_committed
   - Trend chart: cycle_time_avg over sprints (Chart.js line)
   - InsightStrip for BOTTLENECK and HUMAN_VS_AGENT alerts
   - Phase breakdown bar chart: phase_avg_hrs
   - Uses useCurrentMetrics + useSprintDetail + useTrends hooks
4. Write failing tests for DoraTab
5. Create `DoraTab.tsx`:
   - KPI cards: deploy_frequency, cycle_time_avg (as lead time proxy), sprint_completion_pct (as change failure proxy)
   - Trend charts for DORA metrics
   - InsightStrip for DORA_GAP alerts
   - Uses useTrends hook
6. axe-core test in every component test
7. Responsive: verify no overflow at 320px

## Constraints
- All data from hooks — no hardcoded values
- Chart.js for all charts
- Responsive at 320px, 768px, 1280px
- axe-core in every test

## Constitutions
- react, typescript, security, testing
