# DASH-010-T002 — Wire Value tab and Story Drill-down tab

## Status: COMPLETE
## Layer: frontend
## Story: DASH-010 — Wire dashboard tabs to real API hooks
## Complexity: S (task 2 of 2)

## Depends on: DASH-010-T001

## AC covered:
- AC-1 (real API hook, not placeholder — Value + Story Drill-down tabs)
- AC-2 (real metrics displayed)
- AC-3 (error state shown, not infinite skeleton)
- AC-9 (Value tab: stories completed, points delivered, trend data)
- AC-10 (Story Drill-down: story list from useSprintDetail, click-through via useStoryDetail)

## Files in Scope:
- MODIFY: `frontend/dashboard/src/pages/ValueTab.tsx`
- MODIFY: `frontend/dashboard/src/pages/StoryDrilldownTab.tsx`
- MODIFY: `frontend/dashboard/tests/unit/ValueTab.test.tsx`
- MODIFY: `frontend/dashboard/tests/unit/StoryDrilldownTab.test.tsx`

## OUT OF SCOPE:
- frontend/dashboard/src/lib/hooks/* (already complete from DASH-008)
- frontend/dashboard/src/components/* (already complete from DASH-009)
- Any backend files
- Tabs already wired in T001

## Implementation steps:

1. **ValueTab** — replace `usePlaceholderValue()` with:
   - `useCurrentMetrics(projectId)` for current sprint KPIs (stories_shipped, sprint_completion_pct, stories_committed)
   - `useTrends(projectId)` for delivery trend data across sprints
   - Map trend data to the delivery trend table
   - Handle loading/error states from both hooks
   - Remove the placeholder interface and hook function

2. **StoryDrilldownTab** — replace `usePlaceholderStoryDrilldown()` with:
   - `useSprintDetail(projectId, selectedSprint)` for story list
   - `useStoryDetail(projectId, selectedSprint, selectedStoryId)` for detail panel
   - Map `SprintDetail.stories` to `StoryRow[]` for `<StoryTable>`
   - Map `StoryMetrics` to `StoryDetail` for `<StoryDetailPanel>`
   - Handle loading/error from both hooks independently
   - Remove the placeholder interface and hook function

3. Write/update unit tests for both tabs:
   - Mock `apiClient.get` to return appropriate data
   - Test: ValueTab renders KPI cards with real data
   - Test: StoryDrilldownTab renders story table, click opens detail panel
   - Test: error states for both tabs
   - Test: axe-core accessibility check

## Constraints:
- ValueTab receives `projectId` as prop from DashboardLayout
- StoryDrilldownTab receives `projectId` and `selectedSprint` as props
- Story drill-down must call useStoryDetail only when a story is selected (conditional fetch)
- axe-core test in every component test
- Match existing code style exactly

## Constitutions: react, testing
