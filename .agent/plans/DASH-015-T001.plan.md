# DASH-015-T001 — SSM Parameter Store module

## Status: COMPLETE
## Layer: infra
## Story: DASH-015 — Terraform SSM Parameter Store module
## Complexity: S (task 1 of 1)

## Depends on: DASH-011-T001

## AC covered:
- AC-1 (DYNAMODB_TABLE as String parameter)
- AC-2 (ANTHROPIC_API_KEY as SecureString parameter)
- AC-3 (CORS_ORIGINS and AWS_REGION as String parameters)
- AC-4 (all values are placeholders — no real secrets)
- AC-5 (README documents commands to set real values)
- AC-6 (checkov: 0 HIGH/CRITICAL)

## Files in Scope:
- MODIFY: `infra/modules/ssm/main.tf`
- MODIFY: `infra/modules/ssm/variables.tf`
- MODIFY: `infra/modules/ssm/outputs.tf`
- MODIFY: `infra/README.md` (add SSM parameter setup section)

## Implementation steps:
1. Create aws_ssm_parameter for each config value:
   - /{project}/{env}/DYNAMODB_TABLE (String, value = "CHANGE_ME")
   - /{project}/{env}/ANTHROPIC_API_KEY (SecureString, value = "CHANGE_ME")
   - /{project}/{env}/CORS_ORIGINS (String, value = "CHANGE_ME")
   - /{project}/{env}/AWS_REGION (String, value = "us-east-1")
   - /{project}/{env}/LOG_LEVEL (String, value = "INFO")
2. Define variables: project, environment, tags
3. Define outputs: parameter_arns (map)
4. Add section to infra/README.md with aws ssm put-parameter commands

## Constraints:
- ANTHROPIC_API_KEY must be SecureString — never String
- All values must be placeholder "CHANGE_ME" — never real secrets
- Parameter path: /{project}/{environment}/{NAME}

## Constitutions: terraform

## Role annotations
[ARCHITECT G2 2026-03-30]: Plan adds LOG_LEVEL as a 5th parameter — not in the story AC but additive and harmless. Confirm it's intentional before G4.
