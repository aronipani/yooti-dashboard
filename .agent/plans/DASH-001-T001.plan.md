# DASH-001-T001 — Repository class, table patterns, and error types

## Status: COMPLETE
## Layer: database
## Story: DASH-001 — DynamoDB repository layer
## Complexity: M (task 1 of 3)

## Depends on
None — this is the foundational task for the entire backend.

## AC covered
- AC-5 (RepositoryError wrapping — error types defined here)

## Files in Scope

### CREATE
- `services/api_python/src/repository/__init__.py`
- `services/api_python/src/repository/metrics_repository.py` — class skeleton, boto3 client at module level, TABLE_NAME from os.environ
- `services/api_python/src/repository/errors.py` — RepositoryError base, ItemNotFoundError
- `services/api_python/src/repository/keys.py` — PK/SK pattern builders matching ARCH-DASHBOARD.md Section 3

### MODIFY
None

### OUT OF SCOPE
- All Lambda handlers
- All frontend files
- template.yaml

## Implementation steps

1. Create `errors.py` with `RepositoryError(Exception)` and `ItemNotFoundError(RepositoryError)`
2. Create `keys.py` with pure functions: `pk(project_id)`, `sk_sprint_summary(num)`, `sk_story(num, story_id)`, `sk_phase(num, story_id)`, `sk_trend(metric, num)`, `sk_current_snapshot()`, `sk_gate(story_id, gate)`, `sk_escalation(story_id, task_id)`
3. Create `metrics_repository.py` with class `MetricsRepository`:
   - Constructor takes optional `boto3.resource` for testability
   - `_table` property using TABLE_NAME from `os.environ`
   - boto3 DynamoDB resource at module level
   - All methods to be added in T002 and T003
4. Write unit tests for key builders and error types

## Constraints
- TABLE_NAME always from os.environ — never hardcoded
- boto3 client at module level — not inside handler
- No Scan anywhere
- All tests use @mock_aws — no real DynamoDB

## Constitutions
- python, aws, security, testing
