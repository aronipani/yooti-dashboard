# DASH-010-T001 — DashboardLayout sprint state + wire 4 current-metrics tabs

## Status: COMPLETE
## Layer: frontend
## Story: DASH-010 — Wire dashboard tabs to real API hooks
## Complexity: S (task 1 of 2)

## Depends on: DASH-009-T004

## AC covered:
- AC-1 (real API hook, not placeholder — all 4 tabs)
- AC-2 (real metrics displayed in KPI cards)
- AC-3 (error state shown, not infinite skeleton)
- AC-4 (sprint selector change triggers data refresh)
- AC-5 (Productivity tab: velocity, cycle time, throughput)
- AC-6 (DORA tab: deploy frequency, lead time, CFR, MTTR)
- AC-7 (Quality tab: coverage, mutation score, security)
- AC-8 (Agent Efficiency tab: iterations, escalation rate, phase durations)

## Files in Scope:
- MODIFY: `frontend/dashboard/src/pages/DashboardLayout.tsx`
- MODIFY: `frontend/dashboard/src/pages/ProductivityTab.tsx`
- MODIFY: `frontend/dashboard/src/pages/DoraTab.tsx`
- MODIFY: `frontend/dashboard/src/pages/QualityTab.tsx`
- MODIFY: `frontend/dashboard/src/pages/AgentEfficiencyTab.tsx`
- MODIFY: `frontend/dashboard/tests/unit/DashboardLayout.test.tsx`
- MODIFY: `frontend/dashboard/tests/unit/ProductivityTab.test.tsx`
- MODIFY: `frontend/dashboard/tests/unit/DoraTab.test.tsx`
- MODIFY: `frontend/dashboard/tests/unit/QualityTab.test.tsx`
- MODIFY: `frontend/dashboard/tests/unit/AgentEfficiencyTab.test.tsx`

## OUT OF SCOPE:
- frontend/dashboard/src/lib/hooks/* (already complete from DASH-008)
- frontend/dashboard/src/components/* (already complete from DASH-009)
- Any backend files

## Implementation steps:

1. **DashboardLayout** — add sprint state management:
   - Add `projectId` constant (from `import.meta.env.VITE_PROJECT_ID` or default `'yooti-dashboard'`)
   - Add `useState<number | null>(null)` for `selectedSprint`
   - Replace the static `<select>` with `<SprintSelector>` from DASH-008
   - Pass `projectId` and `selectedSprint` as props to the active tab component
   - Update DashboardLayout tests for sprint selector integration

2. **ProductivityTab** — replace `usePlaceholderProductivity()` with:
   - `useCurrentMetrics(projectId)` for KPI data
   - Map `CurrentSnapshot` fields to KPI cards: `stories_shipped` → Velocity, `cycle_time_avg_days` → Avg Cycle Time, `stories_committed` → Throughput, `sprint_completion_pct` → WIP
   - Pass `data.insight_alerts` to `<InsightStrip>`
   - Handle loading/error states from the hook
   - Remove the placeholder interface and hook function

3. **DoraTab** — replace `usePlaceholderDora()` with:
   - `useCurrentMetrics(projectId)` for KPI data
   - Map fields: `deploy_frequency`, `cycle_time_avg_days` → Lead Time, `regression_rate_pct` → Change Failure Rate, `human_wait_hrs_avg` → Time to Restore
   - Filter `insight_alerts` for DORA_GAP type
   - Handle loading/error states

4. **QualityTab** — replace `usePlaceholderQuality()` with:
   - `useCurrentMetrics(projectId)` for KPI data
   - Map fields: `coverage_new_code_avg`, `mutation_score_avg`, `regression_rate_pct`, `constitution_pass_pct`
   - Filter `insight_alerts` for QUALITY_DROP, REGRESSION_SPIKE, CONSTITUTION_GAP types
   - Handle loading/error states

5. **AgentEfficiencyTab** — replace `usePlaceholderAgentEfficiency()` with:
   - `useCurrentMetrics(projectId)` for KPI data
   - Map fields: `agent_exec_hrs_avg`, `human_wait_hrs_avg`, `iteration_avg`, `escalation_rate_pct`, `correction_rate_pct`
   - Map `escalation_breakdown` to table rows
   - Filter `insight_alerts` for HUMAN_VS_AGENT, ESCALATION_SPIKE types
   - Handle loading/error states

6. Write/update unit tests for all 4 tabs:
   - Mock `apiClient.get` to return `CurrentSnapshot` data
   - Test: data renders in KPI cards (not skeleton)
   - Test: error state shown on API failure
   - Test: axe-core accessibility check

## Constraints:
- No hardcoded project IDs — use env var or prop
- All tabs receive projectId as a prop from DashboardLayout
- Tab components must handle loading, error, and success states
- axe-core test in every component test
- Match existing code style exactly

## Constitutions: react, testing
