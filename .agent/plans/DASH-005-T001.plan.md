# DASH-005-T001 — get_trends Lambda handler

## Status: COMPLETE
## Layer: api
## Story: DASH-005 — GET /metrics/trends and GET /metrics/story/{id}
## Complexity: M (task 1 of 2)

## Depends on
- DASH-001-T002 (repository read methods must exist)

## AC covered
- AC-1 (5 trend points for cycle_time_avg → array with sprint_num, value, delta_from_prev, direction)
- AC-2 (no metric filter → all metric series grouped by metric_name)

## Files in Scope

### CREATE
- `services/api_python/src/handlers/get_trends.py`
- `services/api_python/src/services/trend_calculator.py` — compute delta_from_prev and direction
- `services/api_python/tests/unit/test_get_trends.py`

### MODIFY
None

### OUT OF SCOPE
- get_story_detail (T002)
- Repository layer (DASH-001)
- All frontend files

## Implementation steps

1. Write failing tests for AC-1 and AC-2
2. Create `trend_calculator.py`:
   - `enrich_trend_series(points: list[dict]) -> list[dict]` — add delta_from_prev, direction (up/down/flat)
   - Sort ascending by sprint_num for charting
3. Create `get_trends.py` handler:
   - Extract project_id and optional metric from event["queryStringParameters"]
   - Validate project_id → 400 INVALID_PROJECT_ID
   - If metric provided: call repository.get_trends(project_id, metric) → AP-5
   - If no metric: call repository.get_trends(project_id) → AP-6
   - Enrich with delta/direction using trend_calculator
   - Group by metric_name
   - Return 200 with { trends: { metric_name: [...] } }
   - Catch RepositoryError → 500
   - CORS headers on all responses
4. boto3 client at module level

## Constraints
- Trend data sorted ascending by sprint_num for charting
- No Scan — Query only
- Handler returns dict — never raises
- All tests use @mock_aws

## Constitutions
- python, aws, security, testing
