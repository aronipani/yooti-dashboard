# DASH-013-T001 — Lambda module with IAM roles

## Status: COMPLETE
## Layer: infra
## Story: DASH-013 — Terraform Lambda and API Gateway module
## Complexity: M (task 1 of 2)

## Depends on: DASH-011-T001, DASH-012-T001

## AC covered:
- AC-1 (all 6 Lambda functions exist)
- AC-2 (IAM roles with least-privilege DynamoDB actions)
- AC-4 (environment variables from SSM)
- AC-6 (terraform validate passes)

## Files in Scope:
- MODIFY: `infra/modules/lambda/main.tf`
- MODIFY: `infra/modules/lambda/variables.tf`
- MODIFY: `infra/modules/lambda/outputs.tf`

## Implementation steps:
1. Read template.yaml Lambda definitions as source of truth
2. Define aws_lambda_function for each of the 6 handlers with Python 3.12, arm64, 256MB, 30s timeout
3. Create IAM execution role per function with least-privilege DynamoDB permissions (read-only for GET handlers, read-write for ingest)
4. Wire TABLE_NAME and AWS_REGION from variables (sourced from SSM in environment layer)
5. Define variables: project, environment, dynamodb_table_name, dynamodb_table_arn, source_code_path
6. Define outputs: function_arns, function_names, invoke_arns

## Constraints:
- No Action=["*"] or Resource=["*"] — specify exact DynamoDB actions per function
- Read handlers: dynamodb:GetItem, dynamodb:Query only
- Ingest handler: dynamodb:PutItem, dynamodb:BatchWriteItem, dynamodb:GetItem, dynamodb:Query
- Handler paths must match template.yaml exactly

## Constitutions: terraform

## Role annotations
[ARCHITECT G2 2026-03-30]: No Lambda packaging strategy. aws_lambda_function requires filename (a .zip) or s3_key + s3_bucket. The plan lists source_code_path as a variable but never defines how the zip is created. Without a data "archive_file" block or pre-built zip reference, terraform plan will fail. memory_size is not listed as a variable. DASH-016 needs to override this to 128MB for dev. If it's hardcoded at 256MB in the module, DASH-016 can't fulfill its own AC.
