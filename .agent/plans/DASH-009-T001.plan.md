# DASH-009-T001 — Shared components: KPI card, chart wrapper, insight strip, trend badge

## Status: COMPLETE
## Layer: frontend
## Story: DASH-009 — Six-tab React dashboard
## Complexity: L (task 1 of 4)

## Depends on
- DASH-008-T001 (hooks and API client must exist)

## AC covered
- AC-1 (KPI cards render with real data — no hardcoded values)
- AC-2 (insight strip renders with correct message and severity colour)
- AC-3 (responsive at 320px — no horizontal scroll)
- AC-5 (zero axe-core violations)

## Files in Scope

### CREATE
- `frontend/dashboard/src/components/KpiCard.tsx` — value, label, trend arrow, colour-coded direction
- `frontend/dashboard/src/components/ChartWrapper.tsx` — Chart.js wrapper with loading/error states
- `frontend/dashboard/src/components/InsightStrip.tsx` — alert list with severity styling (warn/critical/info)
- `frontend/dashboard/src/components/TrendBadge.tsx` — up/down/flat arrow with green/red colour by metric direction
- `frontend/dashboard/src/components/LoadingSkeleton.tsx` — reusable skeleton for cards and charts
- `frontend/dashboard/src/components/ErrorState.tsx` — error message with retry button
- `frontend/dashboard/src/lib/types.ts` — TypeScript interfaces for all API response shapes
- `frontend/dashboard/tests/unit/KpiCard.test.tsx`
- `frontend/dashboard/tests/unit/InsightStrip.test.tsx`
- `frontend/dashboard/tests/unit/TrendBadge.test.tsx`
- `frontend/dashboard/tests/unit/ErrorState.test.tsx`

### MODIFY
None

### OUT OF SCOPE
- Tab components (T002, T003, T004)
- Hooks and API client (DASH-008)
- Python code

## Implementation steps

1. Create `types.ts` with interfaces: SprintSummary, StoryMetrics, TrendPoint, InsightAlert, CurrentSnapshot, SprintDetail
2. Write failing tests for KpiCard: renders value, label, trend arrow; colour logic; accessibility
3. Implement KpiCard — props: { label, value, previousValue, direction, format }
4. Write failing tests for TrendBadge: up-bad=red, down-good=green, flat=neutral
5. Implement TrendBadge — props: { direction, goodDirection }
6. Write failing tests for InsightStrip: renders alerts, severity colours, message text
7. Implement InsightStrip — props: { alerts: InsightAlert[] }
8. Write failing tests for ChartWrapper: loading, error, data states
9. Implement ChartWrapper — props: { type, data, options, isLoading, isError }
10. Implement LoadingSkeleton and ErrorState
11. All components: responsive with Tailwind, no horizontal scroll at 320px
12. axe-core test in every component test

## Constraints
- KPI trend arrows: down-good=green, up-bad=red (by metric direction config)
- Insight alerts styled by severity: warn=yellow, critical=red, info=blue
- All components responsive at 320px, 768px, 1280px
- axe-core test in every component test
- No hardcoded values — all from props

## Constitutions
- react, typescript, security, testing
