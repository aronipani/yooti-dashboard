# DASH-012-T001 — DynamoDB Terraform module

## Status: COMPLETE
## Layer: infra
## Story: DASH-012 — Terraform DynamoDB module
## Complexity: S (task 1 of 1)

## Depends on: DASH-011-T001

## AC covered:
- AC-1 (PK/SK key schema matching template.yaml)
- AC-2 (GSI-1 on project_id + updated_at with ALL projection)
- AC-3 (TTL enabled on ttl attribute)
- AC-4 (Project, Environment, ManagedBy=terraform tags)
- AC-5 (checkov: 0 HIGH/CRITICAL)
- AC-6 (terraform validate passes)

## Files in Scope:
- MODIFY: `infra/modules/dynamodb/main.tf`
- MODIFY: `infra/modules/dynamodb/variables.tf`
- MODIFY: `infra/modules/dynamodb/outputs.tf`

## Implementation steps:
1. Read template.yaml DynamoDB resource as source of truth
2. Implement aws_dynamodb_table in main.tf with PK (S, HASH), SK (S, RANGE), PAY_PER_REQUEST billing, SSE enabled, PITR enabled, TTL on ttl attribute
3. Add GSI-1: project_id (HASH) + updated_at (RANGE), ALL projection
4. Define variables: project, environment, tags
5. Define outputs: table_name, table_arn, gsi_arn

## Constraints:
- Table name: var.project-var.environment-metrics
- Must exactly mirror template.yaml DynamoDB resource
- PITR and encryption mandatory
- All resources tagged

## Constitutions: terraform
