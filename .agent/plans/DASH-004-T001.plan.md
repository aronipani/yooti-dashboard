# DASH-004-T001 — get_sprint_list Lambda handler

## Status: COMPLETE
## Layer: api
## Story: DASH-004 — GET /metrics/sprints and GET /metrics/sprint/{id}
## Complexity: M (task 1 of 2)

## Depends on
- DASH-001-T002 (repository read methods must exist)

## AC covered
- AC-1 (five sprints → array of five summaries in descending sprint_num order)

## Files in Scope

### CREATE
- `services/api_python/src/handlers/get_sprint_list.py`
- `services/api_python/tests/unit/test_get_sprint_list.py`

### MODIFY
None

### OUT OF SCOPE
- get_sprint_detail (T002)
- Repository layer (DASH-001)
- All frontend files

## Implementation steps

1. Write failing tests: 5 sprints → sorted descending, missing project_id → 400
2. Create `get_sprint_list.py` handler:
   - Extract project_id from event["queryStringParameters"]
   - Validate → 400 INVALID_PROJECT_ID if missing
   - Call repository.get_sprint_list(project_id)
   - Sort results by sprint_num descending
   - Return 200 with { sprints: [...] }
   - Catch RepositoryError → 500 INTERNAL_ERROR
   - CORS headers on all responses
3. boto3 client at module level

## Constraints
- No Scan — Query with SK begins_with only (AP-4)
- Results sorted descending by sprint_num
- Handler returns dict — never raises
- All tests use @mock_aws

## Constitutions
- python, aws, security, testing
