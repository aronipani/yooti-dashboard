# DASH-009-T003 — Quality tab and Agent efficiency tab

## Status: COMPLETE
## Layer: frontend
## Story: DASH-009 — Six-tab React dashboard
## Complexity: L (task 3 of 4)

## Depends on
- DASH-009-T002 (DashboardLayout and tab pattern established)

## AC covered
- AC-1 (Quality and Agent efficiency tabs render with real data)
- AC-3 (responsive at 320px)
- AC-5 (zero axe-core violations)

## Files in Scope

### CREATE
- `frontend/dashboard/src/pages/QualityTab.tsx`
- `frontend/dashboard/src/pages/AgentEfficiencyTab.tsx`
- `frontend/dashboard/tests/unit/QualityTab.test.tsx`
- `frontend/dashboard/tests/unit/AgentEfficiencyTab.test.tsx`

### MODIFY
None

### OUT OF SCOPE
- Productivity and DORA tabs (T002)
- Value and Story drill-down tabs (T004)
- Shared components (T001)

## Implementation steps

1. Write failing tests for QualityTab
2. Create `QualityTab.tsx`:
   - KPI cards: coverage_new_code_avg, mutation_score_avg, regression_rate_pct, constitution_pass_pct
   - Trend charts for coverage and regression rate
   - InsightStrip for QUALITY_DROP, REGRESSION_SPIKE, CONSTITUTION_GAP alerts
   - Gate rejection rates breakdown: G1-G5 bar chart
   - Uses useCurrentMetrics + useSprintDetail + useTrends hooks
3. Write failing tests for AgentEfficiencyTab
4. Create `AgentEfficiencyTab.tsx`:
   - KPI cards: agent_exec_hrs_avg, human_wait_hrs_avg, iteration_avg, escalation_rate_pct, correction_rate_pct
   - Agent vs human time comparison chart
   - Escalation breakdown chart: by type (ENV_ERROR, SCOPE_ERROR, etc.)
   - InsightStrip for HUMAN_VS_AGENT, ESCALATION_SPIKE alerts
   - Uses useCurrentMetrics + useSprintDetail + useTrends hooks
5. axe-core test in every component test
6. Responsive: verify no overflow at 320px

## Constraints
- All data from hooks — no hardcoded values
- Chart.js for all charts
- Responsive at 320px, 768px, 1280px
- axe-core in every test

## Constitutions
- react, typescript, security, testing
