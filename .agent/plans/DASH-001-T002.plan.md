# DASH-001-T002 — All read methods with access pattern tests

## Status: COMPLETE
## Layer: database
## Story: DASH-001 — DynamoDB repository layer
## Complexity: M (task 2 of 3)

## Depends on
- DASH-001-T001

## AC covered
- AC-2 (get_sprint_summary — single Query, no Scan)
- AC-3 (get_sprint_stories — SK begins_with, one Query)
- AC-4 (get_current_snapshot — single GetItem)
- AC-5 (ClientError → RepositoryError on all read methods)

## Files in Scope

### CREATE
- `services/api_python/tests/unit/test_repository_reads.py`

### MODIFY
- `services/api_python/src/repository/metrics_repository.py` — add read methods

### OUT OF SCOPE
- Write methods (T003)
- All Lambda handlers
- All frontend files

## Implementation steps

1. Write failing tests for each read method using @mock_aws
2. Implement `get_current_snapshot(project_id)` — single GetItem (AP-1)
3. Implement `get_sprint_summary(project_id, sprint_num)` — GetItem (AP-3)
4. Implement `get_sprint_stories(project_id, sprint_num)` — Query SK begins_with (AP-2)
5. Implement `get_sprint_list(project_id)` — Query SK begins_with SPRINT# + filter SUMMARY (AP-4), sorted descending
6. Implement `get_trends(project_id, metric=None)` — Query (AP-5 or AP-6)
7. Implement `get_story_detail(project_id, sprint_num, story_id)` — GetItem story + Query gates (AP-8) + Query escalations (AP-9)
8. Implement `get_phase_durations(project_id, sprint_num, story_id)` — GetItem (AP-7)
9. Each method: catch ClientError → raise RepositoryError
10. Each method: return None or empty list for not-found (not raise)

## Constraints
- No Scan — every read uses Query or GetItem
- All tests use @mock_aws with moto
- Each test asserts the correct DynamoDB operation type
- TABLE_NAME from os.environ

## Constitutions
- python, aws, security, testing

## Role annotations
[ARCHITECT G2 2026-03-29]: Clarify conditional write scope: CURRENT#SNAPSHOT always overwrites. sprint summary uses conditional on first write only.
