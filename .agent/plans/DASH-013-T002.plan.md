# DASH-013-T002 — API Gateway module with routes

## Status: COMPLETE
## Layer: infra
## Story: DASH-013 — Terraform Lambda and API Gateway module
## Complexity: M (task 2 of 2)

## Depends on: DASH-013-T001

## AC covered:
- AC-3 (all 6 routes match SAM template exactly)
- AC-5 (checkov: 0 HIGH/CRITICAL)
- AC-6 (terraform validate passes)

## Files in Scope:
- MODIFY: `infra/modules/api-gateway/main.tf`
- MODIFY: `infra/modules/api-gateway/variables.tf`
- MODIFY: `infra/modules/api-gateway/outputs.tf`

## Implementation steps:
1. Create aws_api_gateway_rest_api with name and description
2. Define resources and methods for all 6 routes matching template.yaml:
   - GET /metrics/current
   - GET /metrics/sprints
   - GET /metrics/sprint/{sprint_num}
   - GET /metrics/trends
   - GET /metrics/story/{story_id}
   - POST /metrics/ingest
3. Configure Lambda proxy integrations for each route
4. Configure CORS (allow localhost:5173 and production domain)
5. Configure throttling
6. Define variables: project, environment, lambda_invoke_arns, cors_origins
7. Define outputs: api_url, api_id, stage_name

## Constraints:
- Routes must exactly match template.yaml
- CORS must allow the frontend origin
- Throttling configured to prevent abuse
- All resources tagged

## Constitutions: terraform

## Role annotations
[ARCHITECT G2 2026-03-30]: No aws_api_gateway_deployment or aws_api_gateway_stage in the implementation steps. A REST API without a deployment and stage has no invoke URL — the api_url output will be empty and AC-3 can't be verified.
