# DASH-016-T001 — Dev environment wiring

## Status: COMPLETE
## Layer: infra
## Story: DASH-016 — Terraform dev environment wiring
## Complexity: M (task 1 of 1)

## Depends on: DASH-012-T001, DASH-013-T002, DASH-014-T002, DASH-015-T001

## AC covered:
- AC-1 (terraform plan shows clean plan with no errors)
- AC-4 (all resource names follow yooti-dashboard-dev-{resource} pattern)
- AC-5 (terraform plan shows 0 changes after apply — idempotent)
- AC-6 (checkov: 0 HIGH/CRITICAL)

## Files in Scope:
- MODIFY: `infra/environments/dev/main.tf`
- MODIFY: `infra/environments/dev/variables.tf`
- MODIFY: `infra/environments/dev/terraform.tfvars`
- MODIFY: `infra/environments/dev/backend.tf`

## Implementation steps:
1. Configure backend.tf with S3 state bucket for dev
2. Wire all modules in main.tf:
   - module "dynamodb" → infra/modules/dynamodb
   - module "ssm" → infra/modules/ssm
   - module "lambda" → infra/modules/lambda (pass dynamodb outputs)
   - module "api_gateway" → infra/modules/api-gateway (pass lambda outputs)
   - module "frontend" → infra/modules/s3-cloudfront
3. Set dev-specific values in terraform.tfvars: project="yooti-dashboard", environment="dev"
4. Define all required variables in variables.tf

## Constraints:
- Environment files call modules only — no direct resources
- All values flow through variables, never hardcoded
- Dev uses same security controls as prod
- Lambda memory 128MB in dev (cost optimization)

## Constitutions: terraform

## Note:
AC-2 (API Gateway reachable) and AC-3 (CloudFront serves dashboard) are integration tests that require terraform apply against a real AWS account. The agent generates the code; the human runs apply at Gate G3.

## Role annotations
[ARCHITECT G2 2026-03-30]: RISK : Plan sets Lambda memory to 128MB for dev, but this only works if memory_size is a variable in the Lambda module (DASH-013-T001). That variable is currently missing from T001's list — see gap above. These two plans need to be consistent or the dev wiring will fail at apply.
NOTE : The vpc module is scaffolded by DASH-011 but not wired in DASH-016. Intentional for now, but leave a comment in main.tf so it doesn't appear as a dangling empty module.
