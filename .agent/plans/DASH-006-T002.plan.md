# DASH-006-T002 — Vite proxy config, env files, and local dev setup

## Status: COMPLETE
## Layer: infrastructure
## Story: DASH-006 — SAM template wiring
## Complexity: S (task 2 of 2)

## Depends on
- DASH-006-T001 (SAM template must exist)

## AC covered
- AC-2 (all endpoints respond via LocalStack — local dev path)
- AC-3 (Vite proxy forwards /api to LocalStack — no CORS error)

## Files in Scope

### CREATE
- `env.local.json` — Lambda env vars for LocalStack
- `.env.example` — all required env vars documented

### MODIFY
- `frontend/dashboard/vite.config.ts` — add proxy config for /api → localhost:4566

### OUT OF SCOPE
- SAM template (T001)
- Lambda handler code
- React components

## Implementation steps

1. Create `env.local.json` with TABLE_NAME, AWS_ENDPOINT_URL for all Lambda functions
2. Create `.env.example` with all env vars from ARCH-DASHBOARD.md Section 8
3. Modify `frontend/dashboard/vite.config.ts`:
   - Add server.proxy: '/api' → target localhost:4566, rewrite strip /api prefix
   - Match exact config from ARCH-DASHBOARD.md Section 8
4. Verify: Vite dev server at 5173, proxy to LocalStack at 4566

## Constraints
- No hardcoded URLs in component code — all via VITE_API_BASE_URL
- .env.example documents every required variable
- env.local.json matches SAM template function names

## Constitutions
- config, security

## Role annotations
[ARCHITECT G2 2026-03-29]: Add LocalStack setup steps: docker compose up localstack -d and python scripts/create_local_resources.py must run before sam local start-api.
