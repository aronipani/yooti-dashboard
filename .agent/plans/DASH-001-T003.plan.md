# DASH-001-T003 — Write methods with idempotency

## Status: COMPLETE
## Layer: database
## Story: DASH-001 — DynamoDB repository layer
## Complexity: M (task 3 of 3)

## Depends on
- DASH-001-T002

## AC covered
- AC-1 (idempotent write — same sprint_num twice, no duplicate, no error)
- AC-5 (ClientError → RepositoryError on all write methods)

## Files in Scope

### CREATE
- `services/api_python/tests/unit/test_repository_writes.py`

### MODIFY
- `services/api_python/src/repository/metrics_repository.py` — add write methods

### OUT OF SCOPE
- Read methods (T002)
- All Lambda handlers
- All frontend files

## Implementation steps

1. Write failing tests for idempotent writes using @mock_aws
2. Implement `put_sprint_summary(project_id, sprint_num, data)` — PutItem with conditional expression for idempotency
3. Implement `put_story_metrics(project_id, sprint_num, story_id, data)` — PutItem
4. Implement `put_phase_durations(project_id, sprint_num, story_id, data)` — PutItem
5. Implement `put_trend_point(project_id, metric_name, sprint_num, value)` — PutItem
6. Implement `put_current_snapshot(project_id, data)` — PutItem (overwrite always)
7. Implement `put_gate_event(project_id, story_id, gate, data)` — PutItem
8. Implement `put_escalation(project_id, story_id, task_id, data)` — PutItem
9. Implement `batch_write_sprint(project_id, sprint_num, payload)` — BatchWriteItem (AP-10) for all entities in one sprint
10. Test: write same sprint twice → no error, items overwritten
11. Each method: catch ClientError → raise RepositoryError

## Constraints
- Conditional writes where appropriate (idempotency)
- BatchWriteItem for bulk sprint ingest
- All tests use @mock_aws
- TABLE_NAME from os.environ

## Constitutions
- python, aws, security, testing
